import 'package:flutter/material.dart';

class AttandencePage extends StatelessWidget {
  const AttandencePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Attandence Page"),
      ),
    );
  }
}
